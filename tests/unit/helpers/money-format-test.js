import { moneyFormat } from 'ember-marketcloud/helpers/money-format';
import { module, test } from 'qunit';

module('Unit | Helper | money format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moneyFormat([42]);
  assert.ok(result);
});
