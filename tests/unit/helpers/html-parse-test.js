import { htmlParse } from 'ember-marketcloud/helpers/html-parse';
import { module, test } from 'qunit';

module('Unit | Helper | html parse');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = htmlParse([42]);
  assert.ok(result);
});
